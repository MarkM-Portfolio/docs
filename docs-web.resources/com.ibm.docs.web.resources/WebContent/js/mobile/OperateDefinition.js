/**
 * 
 * Summary:
 * 		OperateDefinition is used for defining Operation in page(like: files)
 * Nessary Functions:
 * 		@Fucntion operate , this will be invoked while execOperate is called by user
 * Example:
 * 		operateController.addOperate('operateName',{
 * 			operate: function(data){
 * 				//action code!
 * 				
 * 				return *
 * 			},
 *          //optional
 *          onstateChange: function(newState){
 *          }
 * 		});
 * 		operateController.execOperate('operateName');
 */

