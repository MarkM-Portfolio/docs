<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Project Information -->
  <parent>
    <groupId>com.ibm.docs</groupId>
    <artifactId>Viewer</artifactId>
    <version>1.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <groupId>com.ibm.docs.SmartCloud</groupId>
  <artifactId>Viewer</artifactId>
  <name>SmartCloud Viewer - zip</name>
  <packaging>pom</packaging>

  <properties>
    <build_version>${build.smartcloud.version}</build_version>
    <build_timestamp>${docs.build.timestamp}</build_timestamp>
  </properties> 

  <dependencies>
    <dependency>
      <groupId>com.ibm.docs.SmartCloud</groupId>
      <artifactId>com.ibm.concord.viewer.ear</artifactId>
      <version>${project.version}</version>
      <type>ear</type>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <executions>
          <execution>
            <id>copy and filter viewer-config-sc-template.json to viewer-config.json</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}</outputDirectory>
              <resources>
                <resource>
                  <directory>${viewer.deployment.resources.dir}/config/</directory>
                  <filtering>true</filtering>
                  <includes>
                    <include>viewer-config-sc-template.json</include>
                  </includes>
                </resource>                   
              </resources>
            </configuration>
          </execution>
        </executions>              
      </plugin>    
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <id>Build SmartCloud Viewer.zip</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <descriptors>
                <descriptor>assembly_SmartCloud_Viewer.zip.xml</descriptor>
              </descriptors>
              <finalName>ocs_viewer</finalName>
              <appendAssemblyId>false</appendAssemblyId>
            </configuration>            
          </execution>
        </executions>
      </plugin>      
    </plugins>
  </build>  
</project>