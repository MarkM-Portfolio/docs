<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Project Information -->
  <parent>
    <groupId>com.ibm.docs</groupId>
    <artifactId>DocsConversion</artifactId>
    <version>1.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <groupId>com.ibm.docs.OnPremise</groupId>   
  <artifactId>DocsConversion</artifactId>
  <name>OnPremise: DocsConversion - zip</name>
  <packaging>pom</packaging>

  <dependencies>
    <dependency>
      <groupId>com.ibm.docs</groupId>
      <artifactId>IBMConversionSanity</artifactId>
      <version>${project.version}</version>
      <type>ear</type>
    </dependency>  
    <dependency>
      <groupId>com.ibm.docs</groupId>
      <artifactId>com.ibm.symphony.conversion.service.rest.was.ear</artifactId>
      <version>${project.version}</version>
      <type>ear</type>
    </dependency>
    <dependency>
      <groupId>com.ibm.docs.OnPremise</groupId>
      <artifactId>conversion-version.txt</artifactId>
      <version>${project.version}</version>
      <type>txt</type>
    </dependency>     
  </dependencies>
  
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <id>Build OnPremise DocsConversion.zip</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <descriptors>
                <descriptor>assembly_OnPremise_DocsConversion.zip.xml</descriptor>
              </descriptors>
              <finalName>${project.artifactId}</finalName>
              <appendAssemblyId>false</appendAssemblyId>
            </configuration>        
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>  
  
  
</project>