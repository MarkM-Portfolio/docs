<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Project Information -->
  <parent>
    <groupId>com.ibm.docs</groupId>
    <artifactId>zip</artifactId>
    <version>1.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>DocsApp</artifactId>
  <name>DocsApp - pom</name>
  <packaging>pom</packaging>

  <!-- Modules -->
  <modules>
    <module>OnPremise</module>
    <module>SmartCloud</module>
  </modules>  
  
  <dependencies>
    <dependency>
      <groupId>com.ibm.docs</groupId>
      <artifactId>com.ibm.docs.web.resources.ear</artifactId>
      <version>${project.version}</version>
      <type>ear</type>
    </dependency>
    <dependency>
      <groupId>com.ibm.docs</groupId>
      <artifactId>DocsApp_Plugins</artifactId>
      <version>${project.version}</version>
      <type>zip</type>
    </dependency>     
    <dependency>
      <groupId>com.ibm.docs</groupId>
      <artifactId>com.ibm.docs.deployment</artifactId>
      <version>${project.version}</version>
    </dependency>    
  </dependencies>
  
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>create sample cfg properties file</id>
            <phase>validate</phase>
            <configuration>
              <target>
                <copy file="${docsapp.deployment.resources.dir}/installer/cfg.properties" tofile="${project.build.directory}/cfg.properties.sample" />
                <copy file="${docsapp.deployment.resources.dir}/installer/cfg.node.properties" tofile="${project.build.directory}/cfg.node.properties.sample" />
              </target>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>          
        </executions>
      </plugin>
    </plugins>
  </build>  
</project>