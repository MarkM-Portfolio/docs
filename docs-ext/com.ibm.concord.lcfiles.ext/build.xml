<project name="com.ibm.concord.lcfiles.ext" basedir="." default="deploy">
	
	<target name="deploy" depends="">
        <ear destfile="${build.home}/${ant.project.name}_${build.version}_${build.timestamp}.ear" appxml="${basedir}/META-INF/application.xml">        	
            <fileset dir="${build.home}/" includes="**/*.war"/>
        	<fileset dir="." includes="META-INF/"/>
        </ear>
    </target>
</project>

