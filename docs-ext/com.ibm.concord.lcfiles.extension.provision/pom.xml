<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Project Information -->
  <parent>
    <groupId>com.ibm.docs</groupId>
    <artifactId>docs-ext</artifactId>
    <version>1.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>com.ibm.concord.lcfiles.extension.provision</artifactId>
  <name>com.ibm.concord.lcfiles.extension.provision - jar</name>
  <packaging>pom</packaging>

  <modules>
    <module>OnPremise</module>
    <module>SmartCloud</module>
  </modules> 
  
  <properties>
    <assembly.file>assembly_com.ibm.concord.lcfiles.extension.provision.xml</assembly.file>
    <!-- nls.related -->
    <script_home>${docs.root.dir}/docs-ext/com.ibm.concord.lcfiles.ext.war/buildtools/util/nls_scripts</script_home>
    <build.src>${docs.root.dir}/docs-ext</build.src>
  </properties>
  
  <profiles>
    <profile>
      <id>Profile for merge nls resource</id>
      <activation>
        <activeByDefault>false</activeByDefault>
        <file>
          <exists>${basedir}/${assembly.file}</exists>
        </file>
        <os>
          <family>unix</family>
        </os>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <executions>
              <execution>
                <id>copy nls.resource of docs-ext</id>
                <phase>process-sources</phase>
                <configuration>
    	          <target name="copy.nls.resource">
    	            <exec executable="bash">
    	              <arg value="${script_home}/merge_nls_resource.sh"/>
    	              <arg value="${build.src}"/>
    	            </exec>
    	          </target>
                </configuration> 
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
              <execution>
                <id>copy nls</id>
                <phase>prepare-package</phase>
                <configuration>           
                  <target>                  
                    <copy todir="${project.build.directory}/resources/nls/zh" failonerror="true">
                      <fileset dir="${basedir}/resources/nls/zh-cn" includes="*.js"/>
                    </copy>
                  </target>
                </configuration> 
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>              
            </executions>
          </plugin>
        </plugins>          
      </build>
    </profile>
    <profile>
      <id>Profile for viewer_lc35_osgiextension</id>
      <activation>
        <activeByDefault>false</activeByDefault>
        <file>
          <missing>${basedir}/${assembly.file}</missing>
        </file>
      </activation>    
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-resources-plugin</artifactId>
            <executions>
              <!-- Filter the ${docs.build.timestamp} in MANIFEST.MF-->
              <execution>
                <id>copy and filter MANIFEST.MF to build target</id>
                <phase>prepare-package</phase>
                <goals>
                  <goal>copy-resources</goal>
                </goals>
                <configuration>
                  <outputDirectory>${project.build.directory}</outputDirectory>
                  <resources>
                    <resource>
                      <directory>${project.parent.basedir}/META-INF</directory>
                      <!-- filter the ${docs.build.timestamp} -->
                      <filtering>true</filtering>
                      <includes>
                        <include>MANIFEST.MF</include>
                      </includes>
                    </resource>                   
                  </resources>
                </configuration>
              </execution>
            </executions>              
          </plugin>              
          <plugin>
            <artifactId>maven-assembly-plugin</artifactId>
            <executions>       
              <execution>
                <id>build for assembly_smartcloud_com.ibm.concord.lcfiles.extension.provision</id>
                <phase>package</phase>
                <goals>
                  <goal>single</goal>
                </goals>
                <configuration>
                  <descriptors>
                    <descriptor>${project.parent.basedir}/${assembly.file}</descriptor>
                  </descriptors>
                  <archiverConfig>
                    <manifestFile>${project.build.directory}/MANIFEST.MF</manifestFile>        
                  </archiverConfig>
                  <archive>
                    <addMavenDescriptor>false</addMavenDescriptor>
                    <manifest>                      
                        <addDefaultImplementationEntries>false</addDefaultImplementationEntries>
                        <addDefaultSpecificationEntries>false</addDefaultSpecificationEntries>
                        <addClasspath>false</addClasspath>
                    </manifest>
                  </archive>                  
                  <finalName>${project.artifactId}</finalName>
                  <appendAssemblyId>false</appendAssemblyId>
                </configuration>
              </execution>
            </executions>
          </plugin>	  
        </plugins>
      </build>
    </profile>
  </profiles>
    
</project>