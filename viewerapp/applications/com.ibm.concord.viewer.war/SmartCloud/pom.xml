<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Project Information -->
  <parent>
    <groupId>com.ibm.docs</groupId>
    <artifactId>com.ibm.concord.viewer.war</artifactId>
    <version>1.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <groupId>com.ibm.docs.SmartCloud</groupId>
  <artifactId>com.ibm.concord.viewer.war</artifactId>
  <name>SmartCloud: com.ibm.concord.viewer.war - war</name>
  <packaging>war</packaging>

  <properties>
    <webcontent.dir>${project.parent.basedir}/WebContent</webcontent.dir>
    <file.webXml>${project.build.directory}/web.xml</file.webXml>
  </properties>
 
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>update the web.xml for smartcloud</id>
            <phase>validate</phase>
            <configuration>
              <target>
                <property name="webcontent.dir" value="../WebContent"/>
                <property name="appname" value="&lt;display-name&gt;ViewerApp&lt;/display-name&gt;"/>
	            <property name="tab" value="&#x0009;"/>
	            <property name="lan.filter" value="${tab}&lt;filter&gt;${line.separator}${tab}${tab}&lt;filter-name&gt;LLLanguageFilter&lt;/filter-name&gt;${line.separator}${tab}${tab}&lt;filter-class&gt;language.filter.LanguageFilter&lt;/filter-class&gt;${line.separator}${tab}&lt;/filter&gt;${line.separator}${tab}&lt;filter-mapping&gt;${line.separator}${tab}${tab}&lt;filter-name&gt;LLLanguageFilter&lt;/filter-name&gt;${line.separator}${tab}${tab}&lt;url-pattern&gt;/*&lt;/url-pattern&gt;${line.separator}${tab}&lt;/filter-mapping&gt;${line.separator}"/>
	            <property name="ext.filter" value="${tab}&lt;filter&gt;${line.separator}${tab}${tab}&lt;filter-name&gt;ExtensibleFilter&lt;/filter-name&gt;${line.separator}${tab}${tab}&lt;filter-class&gt;com.ibm.lconn.core.web.extension.ExtensibleFilter&lt;/filter-class&gt;${line.separator}${tab}&lt;/filter&gt;${line.separator}${tab}&lt;filter-mapping&gt;${line.separator}${tab}${tab}&lt;filter-name&gt;ExtensibleFilter&lt;/filter-name&gt;${line.separator}${tab}${tab}&lt;url-pattern&gt;/*&lt;/url-pattern&gt;${line.separator}${tab}&lt;/filter-mapping&gt;${line.separator}"/>                
                <copyfile src="../WebContent/WEB-INF/web.xml" dest="${project.build.directory}/web.xml" forceoverwrite="true"/>
                <replace file="${project.build.directory}/web.xml" token="${appname}" value="${appname}${line.separator}${lan.filter}"/>
                <replace file="${project.build.directory}/web.xml" token="${appname}" value="${appname}${line.separator}${ext.filter}"/>
              </target>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!--Build the parent src -->
      <plugin>  
        <groupId>org.codehaus.mojo</groupId>  
        <artifactId>build-helper-maven-plugin</artifactId>  
        <executions>
          <execution>
            <id>add-source</id>  
            <phase>generate-sources</phase>  
            <goals>  
              <goal>add-source</goal>  
            </goals>  
            <configuration>  
              <sources>  
                <source>${project.parent.basedir}/src</source>
              </sources>  
            </configuration>  
          </execution>
          <execution>
            <id>add-resources</id>  
            <phase>generate-resources</phase>  
            <goals>  
              <goal>add-resource</goal>  
            </goals>  
            <configuration>  
              <resources>
                <resource>
                  <directory>${project.parent.basedir}/src</directory>
                  <filtering>false</filtering>
                  <excludes>
                    <exclude>**/*.java</exclude>
                  </excludes>          
                </resource>
              </resources>
            </configuration>  
          </execution>           
        </executions>
      </plugin>
    </plugins>
  </build>
</project>